var response = prompt ('Chat it up');

if (response == response.toUpperCase()) {
    console.log('Lower your tone');
} else if ( response == response.toLowerCase()) {
    console.log('Speak up a little');
} else {
     ( response == response.toUpperCase && response == response.toLowerCase()) ;
    console.log('Now you\'re talking my lanuage');
}
